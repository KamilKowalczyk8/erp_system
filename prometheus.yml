global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'erp_backend'
    metrics_path: '/actuator/prometheus'
    params:
      format: ['prometheus']
    static_configs:
      - targets: ['app:8080']
